export default function HomePage() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-4 bg-slate-50 p-6 text-center">
      <h1 className="text-3xl font-bold text-slate-900">Service Income Tracker</h1>
      <p className="max-w-sm text-slate-600">
        Project scaffold is ready. Auth, services, income logging, stats, and import
        arrive in the next tasks.
      </p>
    </main>
  )
}
